<h1>Projects</h1>

<%= link_to "New Project", new_project_path %><br>
<% @projects.each do |proj| %>
  <b>title:</b> <%= proj.title %><br>
  <b>content:</b> <%= proj.content %><br>
  <b>owner:</b> <%= User.find(proj.owner_id).name %><br>
<% end %>

<%= react_component('HelloMessage', name: 'John') %>
